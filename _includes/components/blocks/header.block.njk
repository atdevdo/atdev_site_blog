{% block block_header %}
    <header id="category-filter" style="margin-top: 6rem;">
        <nav id="category-filter__list">
            {% for item in category_links[locale].links %}
                <a href="{{item.link}}">{{item.label}}</a>
            {% endfor %}
        </nav>
    </header>
{% endblock block_header %}